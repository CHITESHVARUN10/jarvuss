[package]
name = "jarvis_core"
version = "0.1.0"
edition = "2021"

[lib]
name = "jarvis_core"
crate-type = ["cdylib", "rlib"]

[dependencies]
regex = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uniffi = "0.28"
